name: site

on:
  push:
    branches: [main, "scaffold/**", "feature/**", "fix/**"]
    paths:
      - "site/**"
      - ".github/workflows/site.yml"
  pull_request:
    branches: [main]
    paths:
      - "site/**"

jobs:
  build:
    name: Build site
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm
          cache-dependency-path: site/package-lock.json

      - name: Install
        working-directory: site
        run: npm ci

      - name: Build
        working-directory: site
        run: npm run build

      - name: Confirm key files exist in dist
        working-directory: site
        run: |
          # Static pages
          test -f dist/index.html
          test -f dist/privacy/index.html
          test -f dist/terms/index.html
          test -f dist/support/index.html
          # Static assets
          test -f dist/robots.txt
          test -f dist/favicon.svg
          test -f dist/_headers
          test -f dist/.well-known/apple-app-site-association
          echo "Static build output OK"

      - name: Confirm Pages Functions exist
        working-directory: site
        run: |
          test -f functions/api/health.ts
          test -f functions/post/[id].ts
          test -f functions/creator/[id].ts
          test -f functions/_lib/renderShareFallback.ts
          echo "Pages Functions present"
